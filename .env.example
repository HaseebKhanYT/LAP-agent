# Root env for `docker compose`. Copy to `.env` and fill in.
# The whole stack boots WITHOUT these; they are only needed for a real learning run.

# Nebius Token Factory (OpenAI-compatible LLM inference).
NEBIUS_API_KEY=
NEBIUS_MODEL=meta-llama/Llama-3.3-70B-Instruct

LOG_LEVEL=INFO
